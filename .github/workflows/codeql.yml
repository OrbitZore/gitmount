# CodeQL security analysis for the C++ codebase.
# SPDX-FileCopyrightText: 2026 The gitmount authors
# SPDX-License-Identifier: GPL-3.0-or-later
name: codeql

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '27 5 * * 3'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  analyze:
    name: analyze (cpp)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5 # v7
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential pkg-config cmake ninja-build \
            libfuse3-dev libgit2-dev
      - name: Initialize CodeQL
        uses: github/codeql-action/init@1190a975f95c # v3
        with:
          languages: cpp
          queries: security-extended
      - name: Build
        uses: github/codeql-action/autobuild@1190a975f95c # v3
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@1190a975f95c # v3
        with:
          category: '/language:cpp'
